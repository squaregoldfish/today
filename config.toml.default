[rtm]
api_key=''
shared_secret=''
token=''
required_lists = ['Work']

[calendar]
    [calendar.myCalendar]
    calendar = 'https://example.org/calendar.ics'
    color = 'blue'

[gtfs]
database = 'gtfs.db'
departure_stops = [1]
arrival_stops = [2]
replacements = [
  ['SEARCH', 'REPLACE']
]
